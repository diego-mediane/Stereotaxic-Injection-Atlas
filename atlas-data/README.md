# Atlas data

This directory contains a placeholder manifest in the source repository. The GitHub Actions workflow replaces it during deployment with browser-ready Allen CCF 2017 assets:

```text
manifest.json
template.uint8.gz
annotation.uint16le.gz
structures.json
```

The generated files are derived from the official Allen 50 µm average-template and annotation volumes. They are not stored in the source repository.
